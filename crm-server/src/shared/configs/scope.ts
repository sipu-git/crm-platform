import { googleScopes } from '../integrations/google/google.scopes.js';

export const googleScope = {
  gmail: googleScopes.gmail,
  calendar: googleScopes.calendar,
  combined: googleScopes.combined,
};