# Page components

This directory contains page components used by the explicit route map in `src/App.tsx`.
React Router defines URLs and nested layouts there; these filenames are not routing conventions.
